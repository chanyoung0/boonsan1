services:
  postgres:
    image: postgres:17-alpine
    container_name: boonsan-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: boonsan
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      TZ: Asia/Seoul
      PGTZ: Asia/Seoul
    ports:
      - "5432:5432"
    volumes:
      - boonsan-pgdata:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d boonsan"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  boonsan-pgdata:
    name: boonsan-pgdata
