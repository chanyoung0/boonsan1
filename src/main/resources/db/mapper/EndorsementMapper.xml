<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="db.mapper.EndorsementMapper">

    <resultMap id="EndorsementResult" type="Endorsement">
        <id     property="endorsementId"   column="endorsement_id"/>
        <result property="policyNumber"    column="policy_number"/>
        <result property="endorsementType" column="endorsement_type"
                javaType="enums.EndorsementType"/>
        <result property="previousContent" column="previous_content"/>
        <result property="newContent"      column="new_content"/>
        <result property="appliedAt"       column="applied_at"/>
        <result property="processedAt"     column="processed_at"/>
    </resultMap>

    <sql id="columns">
        endorsement_id, policy_number, endorsement_type, previous_content,
        new_content, change_reason, underwriting_result, applied_at, processed_at
    </sql>

    <select id="findById" parameterType="string" resultMap="EndorsementResult">
        SELECT <include refid="columns"/>
        FROM endorsement
        WHERE endorsement_id = #{endorsementId}
    </select>

    <select id="findAll" resultMap="EndorsementResult">
        SELECT <include refid="columns"/>
        FROM endorsement
        ORDER BY endorsement_id
    </select>

    <select id="findAllIds" resultType="string">
        SELECT endorsement_id FROM endorsement ORDER BY endorsement_id
    </select>

    <insert id="insert">
        INSERT INTO endorsement
        (endorsement_id, policy_number, endorsement_type, previous_content,
         new_content, change_reason, underwriting_result, applied_at, processed_at)
        VALUES (#{endorsementId}, #{policyNumber}, #{endorsementType},
                #{end.previousContent}, #{end.newContent},
                #{changeReason}, #{uwResult},
                #{end.appliedAt}, #{end.processedAt})
    </insert>

    <update id="update">
        UPDATE endorsement SET
            policy_number       = #{policyNumber},
            endorsement_type    = #{endorsementType},
            previous_content    = #{end.previousContent},
            new_content         = #{end.newContent},
            change_reason       = #{changeReason},
            underwriting_result = #{uwResult},
            applied_at          = #{end.appliedAt},
            processed_at        = #{end.processedAt}
        WHERE endorsement_id = #{endorsementId}
    </update>

    <delete id="delete" parameterType="string">
        DELETE FROM endorsement WHERE endorsement_id = #{endorsementId}
    </delete>

    <select id="findPolicyNumberById" parameterType="string" resultType="string">
        SELECT policy_number FROM endorsement WHERE endorsement_id = #{endorsementId}
    </select>

    <select id="findEndorsementTypeById" parameterType="string" resultType="string">
        SELECT endorsement_type FROM endorsement WHERE endorsement_id = #{endorsementId}
    </select>

</mapper>
